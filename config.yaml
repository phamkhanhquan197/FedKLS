---

common:
  data_type : dirichlet_niid # data_type = data distribution one among ['iid','dirichlet_niid']
  dataset : zh-plus/tiny-imagenet # data_set = data set used one among ['SetFit/20_newsgroups', 'legacy-datasets/banking77', 'fancyzhx/dbpedia_14', 'cifar10', 'cifar100', 'mnist', 'fashion_mnist', 'sasha/dog-food', 'zh-plus/tiny-imagenet', 'Mike0307/MNIST-M', 'flwrlabs/usps']
  dirichlet_alpha : 0.1 #dirichlet concentration parameter (only used if data_type is dirchlet-niid)
  target_acc : 0.99
  model : ResNet34Pretrained # one among [distilbert-base-uncased, bert-base-uncased, meta-llama/Llama-2-7b-hf, Net, CifarNet, SimpleCNN, KerasExpCNN, MNISTCNN, SimpleDNN, FMCNNModel,FedAVGCNN,Resnet18, Resnet34,ResNet18Pretrained, ResNet34Pretrained,ResNet18Small, ResNet20Small,MobileNetV2,EfficientNetB0,LSTMModel]
  # model: distilbert-base-uncased
  # model: Qwen/Qwen1.5-0.5B
  optimizer : sgd # one among [sgd,adam]
  sgd_momentum : 0.9
  seed : 123  #123 456 789
  multi_node : False
  save_log : True
  num_train_thread: 10 #Save the most memory when setting to 1
  num_test_thread: 10

server:
  num_rounds : 1000
  address : 127.0.0.1
  fraction_fit : 0.3 # fraction of clients to be selected for training
  min_fit_clients: 3 # minimum number of clients to be selected for training
  num_clients : 10 # total number of clients participating in training
  fraction_evaluate : 1 # fraction of clients to be selected for evaluation
  min_evaluate_clients: 10 # minimum number of clients to be selected for evaluation
  strategy : FedAvg #[FedAWA, FedKLSVD, FedAvg, FedLaw, FedProx, FedAvgM, FedOpt, FedAdam, FedMedian, PowD, Scaffold, FedNova]

client:
  loss: FocalLoss # [CrossEntropyLoss, FocalLoss]
  epochs : 1
  batch_size : 64
  test_batch_size : 64
  lr: 0.001 #  [0.00002, 0.0001,0.001, 0.005, 0.01, 0.1, 0.2] 0.005->distilbert-base-uncased, 0.001->bert-base-uncased, 0.0001->Qwen/Qwen1.5-0.5B
  save_train_res : True
  total_cpus : 20 # no. of CPU cores that are assigned for all simulation
  total_gpus : 1 # no. of GPU's assigned for whole simulation
  gpu : True  # True or Faslse, Use GPU for training or not.
  num_cpus : 5  # no. of CPU cores that are assigned for each actor
  num_gpus : 0.25 #  no. of GPU that are assigned for each actor (it can be fraction value as well)

peft:
  enabled: True  #False = FFT, do not set method = fedkls when running FFT
  rank: 32
  alpha: 32 #Equal to the rank
  method: pissa # ['pissa', 'milora', 'middle', 'fedkls', 'lora']

fedawa_config:
  server_valid_ratio: 0.2 
  server_epochs: 1       
  server_optimizer: adam   
  server_lr: 0.05
  gamma: 1.0            
  reg_distance: cos       

fedlaw_config:
  server_funct : 'exp' #['exp' or 'quad'] #currently exp only is implemented
  server_optimizer : 'adam'
  server_valid_ratio : 0.02 #percentage of data used for server side retraining
  server_epochs : 10

fedprox:
  proximal_mu : 0.01

powd_config:
  candidate_client_set: 20 # the d parameter (client fraction ≤ d ≤ total clients)

# NEW: Dynamic Data Updates Configuration
dynamic_data:
  enabled: false  # Enable/disable dynamic data updates
  mode: "incremental"  # "reset" or "incremental"
  round_step: 10  # Dataset update frequency (trigger reload every N rounds)
  val_ratio: 0.2  # Validation split ratio (e.g., 0.2 = 20% validation)
  start_fraction: 0.3  # For incremental: initial data fraction per client (e.g., 0.3 = 30%)
  reset_size_range: [0.5, 1.0]  # For reset mode: [min_fraction, max_fraction] for size variation